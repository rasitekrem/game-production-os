{
  "description": "CI produces evidence; it never assesses a gate.",
  "schema": "gate",
  "base": "examples/example-gate-record.json",
  "set": {
    "/assessed_by": {
      "kind": "CI",
      "id": "ci-pipeline"
    }
  },
  "remove": [],
  "expect": "invalid",
  "expect_keyword": "enum"
}
